## Ringkasan

<!-- Jelaskan apa dan mengapa perubahan ini dibuat (Bahasa Indonesia). -->

## Verifikasi

- [ ] `./gradlew :app-sender:assembleDebug :app-receiver:assembleDebug` hijau
- [ ] `./gradlew lintDebug testDebugUnitTest` hijau
- [ ] Perubahan protokol disinkronkan sender & receiver (jika ada)

## Changelog

<!-- Tambahkan entri CHANGELOG.md sesuai aturan (wajib per PR). -->
